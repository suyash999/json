# eBay Live Stream Analyzer — Dependencies
# =========================================
# Install: pip install -r requirements.txt

# ── Core ──────────────────────────────────
streamlit>=1.30.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
pandas>=2.0.0

# ── OCR ───────────────────────────────────
# EasyOCR (primary — works on CPU, good accuracy)
easyocr>=1.7.0
# Fallback: pytesseract (lighter, needs Tesseract binary installed separately)
# pytesseract>=0.3.10

# ── NLP / Theme Analysis ─────────────────
scikit-learn>=1.3.0

# ── Optional: Local LLM ─────────────────
# For Apple Silicon (Metal acceleration):
#   CMAKE_ARGS="-DLLAMA_METAL=on" pip install llama-cpp-python
# For CPU only:
#   pip install llama-cpp-python
# llama-cpp-python>=0.2.50
